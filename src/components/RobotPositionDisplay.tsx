import React from 'react';

const RobotPositionDisplay: React.FC = () => {
    return (
        <div>
            {/* Coordinate display */}
        </div>
    );
};

export default RobotPositionDisplay;